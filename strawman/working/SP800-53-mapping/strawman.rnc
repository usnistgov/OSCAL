default namespace = "http://scap.nist.gov/schema/oscal"

start = element control-set { control+ }
control =
  element control {
    prop*,
    element title { text }?,
    ( prop | stmt | group )*
  }

prop =
  element prop {
    attribute name { xsd:NCName },
    text
  }

stmt =
  element stmt {
    attribute name { xsd:NCName },
    (prop | stmt | group)*,
    ((\list | p)*) }
       
group = element group { control+ | prop+ | stmt+ | std+ }
# on group, not(nb/@flag != nb/@flag) 

std =  element std {
         attribute href { xsd:anyURI },
         whatnot
  }

p =
  element p {
    whatnot+
  }

\list =
  element list {
    attribute list-type { xsd:NCName },
    element list-item { (p | \list)* }+
  }

whatnot = (text | element italic { text })
